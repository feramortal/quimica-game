body {
    font-family: Arial, sans-serif;
    background-color: #1a1a1a;
    color: #f0f0f0;
    margin: 0;
    padding: 20px;
    text-align: center;
}

#game-container {
    max-width: 1000px;
    margin: 20px auto;
    border: 3px solid #4a2a0a;
    background-color: #2a1a0a;
    padding: 10px;
    border-radius: 10px;
}

header h1 {
    color: #ffcc00;
}

/* Informações dos Jogadores */
.player-info {
    display: flex;
    justify-content: space-around;
    padding: 10px;
    margin-bottom: 10px;
    background-color: #333;
    border-radius: 5px;
}

.player-info button {
    padding: 8px 15px;
    cursor: pointer;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
}

/* Campos de Batalha */
.game-field {
    min-height: 150px;
    border: 2px dashed #444;
    padding: 10px;
    margin: 10px 0;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
}

/* Mão do Jogador */
.player-hand {
    min-height: 180px;
    border: 2px solid #ffcc00;
    background-color: #111;
    padding: 10px;
    margin-top: 20px;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    overflow-x: auto; /* Permite rolar se houver muitas cartas */
}

.player-hand h3 {
    margin: 0 15px;
    color: #ffcc00;
}

/* Estilo da Carta */
.card {
    width: 120px;
    height: 160px;
    background-color: #eee;
    color: #333;
    border: 2px solid #333;
    border-radius: 8px;
    padding: 5px;
    text-align: center;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    user-select: none;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 5px 5px 10px rgba(255, 204, 0, 0.7);
}

.card-cost {
    background-color: #cc0000;
    color: white;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    line-height: 20px;
    margin: 0 auto 5px;
    font-weight: bold;
}

.card-stats {
    display: flex;
    justify-content: space-between;
    font-weight: bold;
    font-size: 1.1em;
}

.card-name {
    font-size: 1.1em;
    font-weight: bold;
    margin-bottom: 5px;
}

/* Área de Mensagens */
.message-area {
    padding: 10px;
    margin-top: 15px;
    background-color: #ffeb3b;
    color: #333;
    border-radius: 5px;
    font-weight: bold;
}

/* Estilos para o estado do jogo */
.current-turn {
    border-color: #00ff00 !important;
    box-shadow: 0 0 10px #00ff00;
}